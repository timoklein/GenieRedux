defaults:
  - /connector@connector: retro_act
  - _self_

connector:
  # Matches the previous pretrain settings (random variant)
  variant: random
  annotation_control_fpath: null
  generator_config:
    n_instances: 1
    n_sessions: 20
    n_steps_max: 500
    n_workers: 10
    output_mode: frame
